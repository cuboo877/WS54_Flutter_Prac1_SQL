import 'package:flutter/material.dart';

class StyleColor {
  static const white = Color(0xFFFFFFFF); // 主色彩-白色
  static const blue = Color(0xFF1E88E5); // 輔助色彩-藍色
  // 考慮到其他配色，我們可以添加如下：
  static const lightgrey =
      Color.fromARGB(255, 91, 90, 90); // 灰色，可以用在不需要強調的文字或背景
  static const black = Color.fromARGB(255, 0, 0, 0);
  static const darkBlue = Color.fromARGB(255, 46, 24, 166);
  static const red = Color.fromARGB(255, 224, 0, 0);
}
